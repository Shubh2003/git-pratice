# Create a workflow that runs on push and prints "Hello World"
# name : question 1

# on : push

# jobs:   
#   build: 
#    runs-on : ubuntu-latest

#    steps:
#      - name : printing
#        run : echo "Hello World"


# name : two steps workflow 
# on : push
# jobs: 
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name : start build
#         run : echo "Starting build"

#       - name : complete build
#         run : echo "build complete"


name : Environment Variable

on : push

env:
  Name : Vaibhavi

jobs :
  build:
    runs-on: ubuntu-latest

    steps:
      - name : printing env Variable
        run : echo "Hello $Name"